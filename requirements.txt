# Estate OS — Python dependencies
# Install with: pip install -r requirements.txt
# Then run: python -m spacy download en_core_web_sm

# Phase 1-2: Capture pipeline, sheets, Gemini
gspread>=6.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.2.0
google-generativeai>=0.8.0
google-genai>=1.0.0

# Phase 5: Vault tokenizer (PII detection + PDF OCR)
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0
pdfplumber>=0.10.0
easyocr>=1.7.0
pypdfium2>=4.0.0
pillow>=10.0.0

# Phase 5: RAG (vector index + search)
lancedb>=0.4.0

# Phase 5: Query interfaces (Streamlit web apps)
streamlit>=1.30.0

# Phase 5: Cloud LLM providers (optional — only needed for claude-tokenized)
anthropic>=0.25.0
openai>=1.10.0

# Estate interview app
customtkinter>=5.2.0
reportlab>=4.0.0
edge-tts>=6.1.0
